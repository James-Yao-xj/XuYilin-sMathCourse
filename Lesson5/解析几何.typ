#import "@preview/cetz:0.4.2"
#import "@preview/tablex:0.0.9"
#import "@preview/ctyp:0.3.0": ctyp
#let (ctypset, cjk) = ctyp()
#let (song, hei, kai, fang) = cjk
#show: ctypset

#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 2cm, right: 2cm),
  header: context [
    #text(size: 8pt, fill: gray)[数学一轮补习讲义]
  ],
)

#set heading(numbering: "1.")

#let ex(n, prob, sol) = [
  #block[
    *例题 #n*：#prob
  ]
  #v(2pt)
  #block[
    *解*：#sol
  ]
  #v(12pt)
]

#align(center)[
  #text(font: "SimSun", size: 24pt, weight: "bold")[高二数学补习 第5课]\
  #v(10pt)
  #text(font: "SimSun", size: 16pt)[——解析几何——]
]

解析几何是考试当中非常具有挑战性的一个章节，不过相比导数而言，解法更加固定，有迹可循。通过足量的练习，是能够保证一定做出来的。需要指出的是，解析几何的题目很有可能有更高级的“背景”，包括但不限于射影几何，仿射变换，线性变换等内容。由于这是联系几何与代数的桥梁，因此还可能与导函数等内容结合考察。多知识点综合的时候难度可能较高。这一讲主要复习三大圆锥曲线的主要性质以及较为简单的计算技巧。具体的题型会在之后的课程中专门介绍。

= 椭圆

== 三种定义
+ 焦点定义：平面内到两个定点的距离之和为常数的点的轨迹。
+ 平面上某个点到一个定点的距离与到一条定直线的距离的比为常数的点的轨迹。
+ 斜率乘积是定值的点的轨迹。

#v(100pt)

== 标准方程
#v(200pt)

== 标准方程按照不同定义的推导
#v(200pt)

#pagebreak()

== 一些简单例题

#ex(
  1,
  [已知椭圆 $x^2/9 + y^2/4 = 1$，求 $a, b, c, e$，焦点、准线方程。],
  [
    与标准式 $x^2/a^2 + y^2/b^2 = 1 (a > b > 0)$ 对比，得
    $a = 3, b = 2$。

    $c = sqrt(a^2 - b^2) = sqrt(9 - 4) = sqrt(5)$，
    离心率 $e = c/a = sqrt(5)/3$。

    焦点为 $F_1(-sqrt(5), 0), F_2(sqrt(5), 0)$。

    准线为 $x = +- a/e = +- a^2/c = +- 9/sqrt(5)$。
  ],
)

#ex(
  2,
  [已知椭圆焦点为 $(+-3, 0)$，长轴长为 $10$，求椭圆方程。],
  [
    焦点在 $x$ 轴上，设方程为 $x^2/a^2 + y^2/b^2 = 1$。

    由 $2a = 10$ 得 $a = 5$；由焦点得 $c = 3$。
    所以 $b^2 = a^2 - c^2 = 25 - 9 = 16$。

    椭圆方程为 $x^2/25 + y^2/16 = 1$。
  ],
)

#ex(
  3,
  [求椭圆 $x^2/25 + y^2/9 = 1$ 在点 $P(4, 9/5)$ 处的切线方程。],
  [
    先验证点在椭圆上：
    $4^2/25 + (9/5)^2/9 = 16/25 + (81/25)/9 = 16/25 + 9/25 = 1$。

    椭圆切线公式：若 $P(x_0, y_0)$ 在 $x^2/a^2 + y^2/b^2 = 1$ 上，
    则切线为 $x x_0/a^2 + y y_0/b^2 = 1$。

    代入 $a^2 = 25, b^2 = 9, x_0 = 4, y_0 = 9/5$：
    $4x/25 + (9/5)y/9 = 1$，
    即 $4x/25 + y/5 = 1$，
    化为 $4x + 5y = 25$。
  ],
)

#ex(
  4,
  [已知椭圆一个焦点为 $(2, 0)$，离心率 $e = 1/2$，求椭圆方程。],
  [
    焦点在 $x$ 轴上，设 $x^2/a^2 + y^2/b^2 = 1$，
    则 $c = 2$，且 $e = c/a$。

    由 $2/a = 1/2$ 得 $a = 4$。
    所以 $b^2 = a^2 - c^2 = 16 - 4 = 12$。

    椭圆方程为 $x^2/16 + y^2/12 = 1$。
  ],
)

= 双曲线

== 两种定义
+ 焦点定义：平面内到两个定点的距离之差为常数的点的轨迹。
+ 平面上某个点到一个定点的距离与到一条定直线的距离的比为常数的点的轨迹。

#v(100pt)

== 标准方程
#v(200pt)

== 标准方程按照不同定义的推导
#v(200pt)

#pagebreak()

== 一些简单例题

#ex(
  1,
  [已知双曲线 $x^2/16 - y^2/9 = 1$，求 $a, b, c, e$，焦点、渐近线、准线方程。],
  [
    与标准式 $x^2/a^2 - y^2/b^2 = 1$ 对比得 $a = 4, b = 3$。

    双曲线满足 $c^2 = a^2 + b^2$，
    所以 $c = sqrt(16 + 9) = 5$，$e = c/a = 5/4$。

    焦点为 $F_1(-5, 0), F_2(5, 0)$。

    渐近线为 $y = +- (b/a) x = +- (3/4)x$。

    准线为 $x = +- a/e = +- a^2/c = +- 16/5$。
  ],
)

#ex(
  2,
  [已知双曲线焦点为 $(+-5, 0)$，实轴长为 $6$，求双曲线方程。],
  [
    设方程为 $x^2/a^2 - y^2/b^2 = 1$。
    由实轴长 $2a = 6$ 得 $a = 3$；由焦点得 $c = 5$。

    $b^2 = c^2 - a^2 = 25 - 9 = 16$。

    双曲线方程为 $x^2/9 - y^2/16 = 1$。
  ],
)

#ex(
  3,
  [求双曲线 $x^2/16 - y^2/9 = 1$ 在点 $P(5, 9/4)$ 处的切线方程。],
  [
    验证点在双曲线上：
    $5^2/16 - (9/4)^2/9 = 25/16 - (81/16)/9 = 25/16 - 9/16 = 1$。

    切线公式：若 $P(x_0, y_0)$ 在 $x^2/a^2 - y^2/b^2 = 1$ 上，
    则切线为 $x x_0/a^2 - y y_0/b^2 = 1$。

    代入 $a^2 = 16, b^2 = 9, x_0 = 5, y_0 = 9/4$：
    $5x/16 - (9/4)y/9 = 1$，
    即 $5x/16 - y/4 = 1$，
    化为 $5x - 4y = 16$。
  ],
)

= 抛物线

== 定义
+ 平面上某个点到一个定点的距离与到一条定直线的距离相等的点的轨迹。

#v(100pt)

== 标准方程
#v(200pt)

== 标准方程按照定义的推导
#v(200pt)

#pagebreak()

== 一些简单例题

#ex(
  1,
  [已知抛物线 $y^2 = 8x$，求焦点与准线方程。],
  [
    标准式为 $y^2 = 4a x$，对比得 $4a = 8$，所以 $a = 2$。

    焦点为 $F(a, 0) = (2, 0)$，
    准线为 $x = -a = -2$。
  ],
)

#ex(
  2,
  [已知抛物线焦点为 $(0, 1)$，准线为 $y = -1$，求抛物线方程。],
  [
    轴为 $y$ 轴且开口向上，设方程为 $x^2 = 4a y$。
    其焦点为 $(0, a)$，准线为 $y = -a$。

    由题设得 $a = 1$，所以抛物线方程为 $x^2 = 4y$。
  ],
)

#ex(
  3,
  [求抛物线 $y^2 = 4x$ 在点 $P(1, 2)$ 处的切线方程。],
  [
    $y^2 = 4x$ 即 $y^2 = 4a x$，其中 $a = 1$。

    设参数 $t$，抛物线上点可表示为 $(x, y) = (t^2, 2t)$。
    由 $P(1, 2)$ 得 $t = 1$。

    切线公式为 $t y = x + t^2$。
    代入 $t = 1$ 得 $y = x + 1$。
  ],
)

#ex(
  4,
  [判断直线 $y = 2x + 1$ 与抛物线 $y^2 = 8x$ 的位置关系，并求公共点。],
  [
    联立：$(2x + 1)^2 = 8x$，
    得 $4x^2 + 4x + 1 = 8x$，
    即 $4x^2 - 4x + 1 = 0$，
    因式分解为 $(2x - 1)^2 = 0$。

    所以 $x = 1/2$，代入直线得 $y = 2$。

    只有一个公共点，故直线与抛物线相切，切点为 $(1/2, 2)$。
  ],
)